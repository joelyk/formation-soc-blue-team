# Exercice 4 — Commandes réseau en terminal

Lié à : Chapitre 4 — Adressage IP et sous-réseaux

## Consigne

Exécuter les commandes suivantes et analyser leur sortie.

| Commande | Ce qu'elle montre | Observation |
|----------|---------------------|-------------|
| `ping <domaine>` | Latence, perte de paquets | |
| `tracert <domaine>` (Windows) / `traceroute` (Linux) | Chemin réseau, sauts intermédiaires | |
| `netstat -an` (Windows) / `ss -tulnp` (Linux) | Connexions actives, ports en écoute | |
| `ipconfig /all` (Windows) / `ip a` (Linux) | Adressage IP local, passerelle | |

## Question

Parmi les ports en écoute observés, lesquels reconnais-tu (22, 80, 443, 445, 3389…) et à quel service correspondent-ils ?

**Réponse :**
